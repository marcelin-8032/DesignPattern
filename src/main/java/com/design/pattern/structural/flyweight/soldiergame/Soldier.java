package com.design.pattern.structural.flyweight.soldiergame;

interface Soldier {

    void render(int x, int y, int health); // Method to render the soldier at specific coordinates with a health status
}
